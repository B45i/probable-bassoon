node_modules/
.turbo/
dist/
.env
.env.*
!.env.example
.dev.vars
.DS_Store

# Cloudflare local dev state (D1 sqlite, KV, DO storage)
.wrangler/

# Generated, not authored — regenerate via `turbo run openapi generate`
apps/*/openapi.json
apps/assignment/src/generated/
.wrangler-state/*
docs/design.simple.md

# packages/perf's own throwaway test data, from `pnpm seed` — not fixtures to check in
packages/perf/.perf-seed.json
*.log
